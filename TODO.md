# My Haskell Project TODOs

- [ ] Implement Hutton's Optimization for `Add` and `Mul` in `CountDown.hs` (`valid Add x y = x <= y`, `valid Mul x y = x <= y && x /= 1 && y /= 1`).
- [ ] Define the `Expr` (AST) data type for Countdown.
